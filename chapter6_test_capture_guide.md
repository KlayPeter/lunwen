# 第六章测试过程截图补拍说明

本文档用于替换第六章中原先不合适的 AI 生成“测试过程图”。当前论文正文已经保留了 4 处占位图，后续只需要按下面步骤完成真实验证并截图，再把截图替换进去即可。

## 一、对应位置

- Web 端聊天与聊天室测试过程图
- Web 端 AI 问答与总结测试过程图
- Web 端收藏功能测试过程图
- 桌面端功能测试过程图

## 二、测试前准备

建议统一使用 `ch6` 前缀创建测试数据，保证与论文正文描述一致。

- 测试账号：`ch6userA`、`ch6userB`
- 公开聊天室：`ch6-public-room`
- 密码聊天室：`ch6-pass-room`
- AI 测试消息：提前准备 4 条和 Vue 3 组件通信相关的文本消息

建议先确认以下环境已经启动：

- 后端服务：`node server.js`
- Web 端：`http://localhost:5173`
- 数据库：本地 MongoDB
- 桌面端：`ccb/dist-electron/win-unpacked/Mini Chat Bar.exe`

## 三、截图总原则

- 截图必须来自你自己实际运行的系统界面，不要再使用生成图。
- 每张“过程图”建议最终做成一张拼图，推荐 `2x2` 或 `3x1` 布局。
- 截图中尽量保留房间名、用户名、状态提示、地址栏、托盘提示或时间信息，提高可信度。
- 右键菜单、托盘悬停窗口这类容易消失的界面，建议使用 Windows 截图工具的延时截图。

## 四、Web 端聊天与聊天室测试过程图

这一张图要证明以下 4 个过程确实跑通过：

1. 私聊发送与回读
2. 公开聊天室创建
3. 密码聊天室加入
4. 空消息拦截

建议按下面顺序操作并截图：

1. 使用 `ch6userA` 和 `ch6userB` 登录系统。
2. 在私聊界面发送一条测试消息，截取“消息刚发送成功”的界面。
3. 刷新或重新进入会话，截取“历史消息仍能回读”的界面。
4. 进入聊天室页面，创建 `ch6-public-room`，截取“创建成功且房间出现在列表中”的界面。
5. 进入密码聊天室 `ch6-pass-room`，输入正确密码，截取“加入成功或系统加入消息”的界面。
6. 在输入框中只输入空格，尝试发送，截取“空白消息被拦截、未真正发送”的界面。

建议最终拼图包含：

- 私聊发送成功
- 私聊历史回读
- 公开聊天室创建成功
- 密码聊天室加入或空消息拦截

## 五、Web 端 AI 问答与总结测试过程图

这一张图要证明以下过程真实发生：

1. 普通 AI 问答
2. `@AI` 快捷提问
3. 讨论总结
4. AI 结果落库后可回读

建议按下面顺序操作并截图：

1. 先向测试聊天室写入 4 条样本消息。
2. 触发普通 AI 问答，先截取“AI 正在思考”的界面。
3. 等返回结果后，再截取“AI 回复已插入消息区”的界面。
4. 使用 `@AI` 方式再提一个问题，截取“用户提问 + AI 回复”的界面。
5. 打开讨论总结功能，截取“总结弹窗或总结结果”的界面。
6. 刷新页面后重新进入聊天室，截取“AI 回复仍能从历史消息中读取”的界面。

建议最终拼图包含：

- AI 正在思考
- AI 回复结果
- `@AI` 快捷提问结果
- 总结结果或刷新后的历史回读

## 六、Web 端收藏功能测试过程图

这一张图要证明以下闭环：

1. 收藏动作触发
2. 收藏列表回读
3. 收藏状态回显
4. 取消收藏
5. 组合筛选结果

建议按下面顺序操作并截图：

1. 在聊天室消息上右键，执行收藏，截取“右键菜单或收藏成功提示”。
2. 进入收藏页，截取“刚收藏的消息已出现在列表中”。
3. 返回聊天室，截取“该消息仍显示为已收藏状态”。
4. 执行取消收藏，截取“收藏消失或状态恢复”的界面。
5. 进入收藏页执行组合筛选。

组合筛选这里分两种情况：

- 如果仍能复现 `500` 错误，就打开浏览器开发者工具的 `Network`，截取失败请求和 `500` 状态。
- 如果已经不能复现 `500`，那就截取“组合筛选正常返回结果”的界面，同时后续需要把论文正文对应异常描述改掉，不能再保留“返回 500”这句话。

建议最终拼图包含：

- 收藏动作
- 收藏列表回读
- 取消收藏或状态回显
- 组合筛选结果

## 七、桌面端功能测试过程图

这一张图要证明以下过程：

1. 程序启动并登录
2. 托盘驻留
3. 未读提示或角标变化
4. 托盘悬停预览
5. 点击预览后恢复窗口并定位会话

建议按下面顺序操作并截图：

1. 启动 `Mini Chat Bar.exe` 并登录，截取主界面。
2. 点击关闭按钮，让程序最小化到托盘，截取托盘图标仍存在的界面。
3. 从另一端给当前账号发送新消息，截取托盘提示、任务栏角标或未读变化。
4. 鼠标悬停托盘图标，截取预览窗口。
5. 点击预览中的某条消息，截取“窗口恢复并跳转到对应会话”的界面。

建议最终拼图包含：

- 启动登录成功
- 托盘驻留
- 未读提示或角标
- 托盘预览或点击跳转结果

## 八、建议文件命名

为了后面替换论文图片方便，建议你最终整理好的图片按下面名字保存：

- `图6_聊天与聊天室测试过程.png`
- `图6_AI测试过程.png`
- `图6_收藏测试过程.png`
- `图6_桌面端测试过程.png`

如果你想继续沿用论文当前写法，也可以直接命名为：

- `图6_chatroom_process.png`
- `图6_ai_process.png`
- `图6_favorite_process.png`
- `图6_desktop_process.png`

## 九、完成后下一步

你把真实截图补齐后，可以继续做两件事：

1. 直接把 4 张真实图替换到论文第六章对应位置。
2. 如果截图内容和正文描述不完全一致，再同步修改第六章相关文字，保证“图、表、文字、测试结果”一致。

如果你把截图文件放进 `img` 目录，我可以继续帮你把 `chapter6.tex` 里的占位图改回正式插图，并顺手把配套说明文字一起修正。
