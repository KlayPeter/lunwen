# 论文核查结论

## 1. 核查范围与依据

本次核查仅做**验证，不修改论文正文**。检查依据包括：

- 论文源文件：`main.tex`、`chapter1.tex` 至 `chapter7.tex`
- 模板目录：`hzucse-Latex/`
- 学校规范：`惠州学院本科生毕业论文（设计）撰写规范与格式要求(2024年修订）.docx`
- 已生成成品：`main.pdf`
- 编译日志：`main.log`、`_compile_cite_redistribute_pass3.log`、`_compile_citefix_pass3.log`、`_compile_reforder_pass3.log`
- 图示源文件：`img 2/*.svg`
- 图生成脚本：`generate_chapter5_standard_flowcharts.py`

说明：

- 当前工作区是论文工程，并**不包含独立的系统源码仓库**（如 `ccb/`、`server/` 等实际项目目录），因此“与作品相符”这一项，我只能按**论文内部一致性**、图示说明、接口/测试描述来核查，**不能替代对真实项目源码的逐文件对照**。

## 2. 总体结论

总体上，这篇论文的**章节结构、图表基本排版、跨页续表、总结章节、主要业务主线**已经比较完整，达到可以继续精修的状态。

但目前仍有 **4 个明确问题** 和 **3 个格式/一致性风险项**，不建议直接作为最终定稿提交。

### 2.1 明确问题

1. **关键词数量超出学校规范**
   - 学校规范写明关键词应为 **3～5 个**。
   - 当前中文关键词有 6 个，英文关键词也有 6 个。
   - 位置：
     - `main.tex:67`
     - `main.tex:78`

2. **第 5 章出现了需求与设计中未充分铺垫的功能，前后存在越界**
   - 第 3 章核心需求是“用户管理、即时通讯、聊天室、AI 助手、收藏”五大模块。
   - 但第 5 章又出现了：
     - `语音交流 / 语音连麦`
     - `代码协作`
     - `问题追踪`
     - `链接预览`
     - `聊天室实时洞察与状态分析`
     - `insights / aiSpeech`
   - 这些内容在第 3 章需求分析和第 4 章总体设计中都**没有形成同等级的需求闭环**，容易被老师认定为“实现部分突然多出功能”。
   - 位置：
     - `chapter5.tex:143`
     - `chapter5.tex:165`
     - `chapter5.tex:169`
     - `chapter5.tex:196`
     - `chapter5.tex:223`

3. **第 6 章“聊天室代码协作测试”与第 7 章展望中的表述发生冲突**
   - 第 6 章把“聊天室代码协作测试”作为已展示证据之一。
   - 但第 7 章又写“多人代码协同、任务跟踪、知识归档等能力，目前还没有形成完整方案”。
   - 这会让评阅人难以判断：代码协作到底是**已实现功能**、**局部实验能力**，还是**未来扩展方向**。
   - 位置：
     - `chapter6.tex:283`
     - `chapter6.tex:284`
     - `chapter7.tex:29`

4. **ER 图存在概念关系表达问题，概念设计与逻辑设计的映射不够严谨**
   - 当前 ER 图更接近“简化数据模型图”，不是很标准的概念 ER 表达。
   - 至少有两处关系表达值得判定为问题：
     - `Users` 与 `Room` 被写成一条“创建 / 加入”关系，但“创建”应是 **1 对多**，“加入”应更接近 **多对多**，两种关系被混写在一条线上，不够规范。
     - `Users` 与 `AIConversation` 的基数在图中写成 **N : 1**，方向明显可疑；按论文正文语义，更合理的是“一个用户可拥有多轮 AI 对话”。
   - 这会削弱“概念设计（ER 图）能推出逻辑结构（表结构）”的说服力。
   - 位置：
     - `chapter4.tex:180`
     - `chapter4.tex:192`
     - `img 2/图4_10_系统数据库E_R图.svg:8`
     - `img 2/图4_10_系统数据库E_R图.svg:12`
     - `img 2/图4_10_系统数据库E_R图.svg:14`
     - `img 2/图4_10_系统数据库E_R图.svg:16`
     - `img 2/图4_10_系统数据库E_R图.svg:17`
     - `img 2/图4_10_系统数据库E_R图.svg:18`

## 3. 风险项

1. **主文档与模板示例的单双面设置不一致**
   - 当前主文档使用：
     - `main.tex:7` `\documentclass[oneside]{thesis-cls-Miya}`
   - 模板示例 `hzucse-Latex/hzu.tex` 使用的是 `twoside`。
   - 学校规范又写到：中英文摘要、正文、参考文献、附录实行**双面打印**。
   - 因此这里至少属于**与模板示例不一致**，是否构成最终格式错误，需要以学院送审要求为准；但我建议把它当成一个**提交前必须再次确认**的风险点。

2. **当前类文件不是模板原样，已做过定制**
   - 与 `hzucse-Latex/thesis-cls-Miya.cls` 对比后，当前 `thesis-cls-Miya.cls` 至少改动了：
     - 图表/公式编号样式
     - 参考文献环境
     - 列表缩进
     - 封面/logo 路径
     - 页眉年份文案
   - 这些改动不一定全部错误，但说明当前论文**并非完全按模板原样输出**，因此模板一致性上存在审查风险。

3. **少数图片引用方式不够规范**
   - 所有表格都已显式 `\ref` 引用。
   - 图大部分也有引用，但有 4 个图标签没有被显式 `\ref`：
     - `fig:2-1`
     - `fig:2-2`
     - `fig:5-6`
     - `fig:5-13`
   - 其中：
     - 第 2 章是直接写“图2-1”“图2-2”，而不是 `图~\ref{...}`，这会降低自动编号的稳健性。
       - 位置：`chapter2.tex:7`、`chapter2.tex:16`、`chapter2.tex:52`、`chapter2.tex:61`
     - 第 5 章的 `fig:5-6`、`fig:5-13` 是被“图5-5至图5-7”“图5-12至图5-14”这种范围写法间接覆盖，不算完全漏引，但**不如逐图引用规范**。

## 4. 各检查项结论

### 4.1 论文内容是否有明显错误、错词错句

结论：**未发现明显占位内容残留、未定义引用、未定义文献或编译级错误；但存在范围表述不一致问题。**

已核到的正面情况：

- 编译日志中未检出 `undefined reference`、`undefined citation`、`Overfull/Underfull` 等明显告警。
- 第 7 章有明确的“工作总结”，不是只有空泛展望。

仍需注意：

- 我没有使用外部语法审校器逐句做人类校对，因此“错别字/病句”只能判定为**未发现明显硬伤**，不能替代最后一轮人工精读。
- 当前更突出的不是病句，而是**功能表述越界**。

### 4.2 图是否规范，图框符号是否规范

结论：**第 3、4 章的大部分流程图、用例图、时序图基本规范；ER 图规范性最弱。**

抽查结果：

- `图4-4 用户模块流程图`：使用起止椭圆、处理矩形、判断菱形，属于常见流程图规范表达。
- `图4-6 聊天室流程图`：同样采用标准流程图符号。
- `图4-7 AI 助手时序图`：生命线、激活条、消息箭头表达基本符合 UML 时序图习惯。
- `图3-4 技术交流人群用例图`：参与者、系统边界、用例椭圆、`include/extend` 关系都比较规范。
- 第 5 章流程图生成脚本 `generate_chapter5_standard_flowcharts.py` 明确使用了 `terminator / process / decision / io_box / document / database` 等标准符号。

保留意见：

- `图4-12 系统数据库E-R图` 更像“实体框 + 字段 + 关系线”的简化数据模型图，不是特别标准的概念 ER 图表达。

### 4.3 需求、设计、实现、测试是否前后一致

结论：**主线基本一致，但实现章节有超纲扩展。**

闭环较好的主线：

- 登录 / 会话保持
- 私聊
- 聊天室
- 收藏
- AI 问答
- 讨论总结
- 桌面端提醒

不一致点：

- 第 5 章新增了“语音交流、代码协作、问题追踪、链接预览、聊天室洞察分析”等内容。
- 第 3 章和第 4 章没有对这些内容作同等级铺垫。
- 第 7 章又把“多人代码协同”等写成未来方向，前后口径不统一。

### 4.4 ER 图是否能推出逻辑结构

结论：**实体集合基本能对应后文核心表，但关系设计表达不够严谨。**

正面：

- 图中实体基本能和后文七张核心表对上：
  - Users
  - Room
  - Messages
  - GroupMessage
  - Favorite
  - AIConversation
  - Summary

问题：

- `Users - Room` 把“创建”和“加入”两种不同业务关系揉成一条线。
- `Users - AIConversation` 基数方向可疑。
- 图里已经写了字段，后文又单独进入表结构设计，因此“概念 ER 图”和“逻辑结构图”的边界有些混。

### 4.5 表格式是否正确，跨页是否续表

结论：**这一项整体合格。**

依据：

- 表格源码普遍采用 `booktabs` 三线表形式，未发现竖线表或 `\hline` 堆叠式表格。
- 第 6 章长表使用了：
  - `chapter6.tex:5`
  - `chapter6.tex:6`
  - 多处 `\endfirsthead + \testcasecontinuedmark`
- 在 `main.pdf` 成品文本中，已检出：
  - `续表 6-2`
  - `续表 6-3`
  - `续表 6-4`
  - `续表 6-5`
  - `续表 6-6`
  - `续表 6-8`
  - `续表 6-10`

### 4.6 所有图表是否在文中有引用并有说明

结论：**表格基本合格；图片基本合格但有少量引用写法不够规范。**

正面：

- 所有表标签都能在正文中找到显式 `\ref`。
- 大多数图片前后都有说明文字。

需要改进的地方：

- 第 2 章两张图使用了固定文字“图2-1”“图2-2”，没有使用 `\ref` 自动引用。
- 第 5 章的 `fig:5-6`、`fig:5-13` 属于范围式引用覆盖，不是单图显式引用。

### 4.7 总结中是否有对自己工作的总结

结论：**合格。**

依据：

- `chapter7.tex` 明确分为：
  - `工作总结`
  - `实践体会`
  - `系统不足与展望`
- 其中“工作总结”已经对需求分析、系统设计、实现与测试做了回顾，不是只有套话式结尾。

### 4.8 与模板 `hzucse-Latex` 的格式一致性

结论：**大部分基础版式继承自同一类文件，但存在可见差异，不宜直接判定为“完全按模板”。**

基本一致的部分：

- 仍然使用 `thesis-cls-Miya.cls`
- 图题、表题、目录、页边距、三线表等基础样式总体仍由类文件控制
- 前置部分顺序在成品 PDF 中基本正常：
  - 封面
  - 诚信声明
  - 中文摘要
  - 英文摘要
  - 目录

与模板/规范不一致的部分：

- `main.tex` 使用 `oneside`，模板示例使用 `twoside`
- 当前类文件相对模板类文件有多处定制
- 关键词数量超出学校规范

## 5. 最终建议

如果按“是否可以直接送审/定稿”来判断，我的结论是：

**暂不建议直接定稿。**

优先级建议如下：

1. 先处理**关键词数量**问题。
2. 再处理**第 5 章与第 7 章的功能口径不一致**问题。
3. 再重画或重述**ER 图关系**，至少把 `Users-Room` 和 `Users-AIConversation` 关系讲清楚。
4. 最后检查**单双面设置是否必须按模板 `twoside` 输出**。

如果按“当前稿件是否已经具备较完整基础”来判断，则答案是：

**具备，且表格、长表续表、总结章节、主业务主线已经比较完整。**
