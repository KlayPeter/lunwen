# 性能测试示意图提示词

这份文档提供的是**示意图提示词**，不是“真实测试证据图”的伪造方案。  
如果你使用 AI 生成这些图片，建议在论文中把图名和正文表述改成：

- 并发广播测试**示意图**
- 资源占用监测**示意图**

不要再写成“真实测试日志截图”“真实监测证据图”，否则会与图片来源不一致。

## 一、使用建议

建议你把提示词喂给擅长生成“界面截图风格 / 仪表盘风格 / 开发者工具风格”的图像模型。

为了更像论文插图，统一要求：

- 风格：真实软件界面截图风格，非扁平流程图
- 画面：清晰、整洁、论文可读
- 比例：横向 `16:9`
- 分辨率：至少 `1600x900`
- 语言：界面内尽量使用中文，少量英文术语保留
- 避免：卡通风、夸张发光、赛博朋克、过度艺术化

## 二、图6-4：并发广播测试日志示意图

### 目标

表现“同一聊天室 100 个 Socket 客户端接入后，由一个客户端发起群消息广播，控制台统计平均时延、P95、最大值和成功率”的效果。

### 正向提示词

```text
生成一张真实软件测试界面风格的性能测试示意图，横向 16:9 构图，论文插图风格，清晰、克制、接近真实开发环境截图。画面由左右两个部分组成：

左侧是 Windows PowerShell / Terminal 深色终端窗口，显示 Node.js 聊天系统服务端日志，内容包括：
Mini Chat Bar server started
MongoDB connected
Socket.IO running on http://localhost:3000
多个客户端连接日志
join-group room=chatroom_ch6_load
online-count count=100
group-message broadcast

右侧是并发广播测试脚本终端输出，要求像真实压测结果汇总界面，清楚显示以下文字和数字：
clients=100
roomId=chatroom_ch6_load
received=100/100
successRate=100%
avg=4ms
p95=5ms
max=5ms
disconnects=0
errors=0

右侧终端上方可以有简洁的“并发广播测试”标题，终端中保留少量 join 进度输出，例如：
[join 1/100]
[join 50/100]
[join 100/100]
[recv 100/100]

整体效果要像毕业论文中的测试执行截图示意，布局规整，文字可读，颜色克制，背景为真实桌面应用界面，不要做成流程图，不要插画风，不要夸张特效。
```

### 负向提示词

```text
不要卡通，不要插画，不要UI概念稿，不要代码雨，不要赛博朋克，不要紫色霓虹，不要模糊，不要英文大段乱码，不要夸张3D，不要不真实的仪表盘，不要手绘风，不要海报风
```

### 备用精简版提示词

```text
真实终端截图风格的并发广播测试示意图，左侧为 Node.js 聊天系统服务端日志，右侧为 PowerShell 并发测试结果，显示 clients=100、received=100/100、successRate=100%、avg=4ms、p95=5ms、max=5ms、disconnects=0、errors=0，论文插图风格，横向 16:9，清晰可读，非流程图。
```

## 三、图6-5：资源占用监测示意图

### 目标

表现“广播压力场景”和“AI 请求场景”下，Node 服务进程 CPU 与内存占用差异”的监测效果。

### 正向提示词

```text
生成一张真实系统监控界面风格的资源占用测试示意图，横向 16:9 构图，适合本科毕业论文插图。画面采用左右双栏布局：

左侧表示“100 客户端广播场景”，右侧表示“AI 问答场景”。

左侧内容为 Windows 任务管理器 + 简洁统计面板的组合效果，突出 Node.js 服务进程在广播压力下的资源占用，要求可见以下关键信息：
标题：100客户端广播场景
CPU 平均占用：2.1%
工作集内存：161MB
私有内存：187MB
持续时间：1.5s
说明：连接数量集中，内存占用上升但整体可控

右侧内容为 AI 请求场景监测界面，显示 Node.js 服务进程在等待外部大模型返回期间的资源占用，要求可见以下关键信息：
标题：AI问答场景
CPU 平均占用：低于1%
工作集内存：93MB
私有内存：118MB
持续时间：31.0s
说明：CPU占用不明显，主要等待远程模型返回

整体风格要求像真实电脑监测截图，可以包含任务管理器、性能折线、小型资源统计卡片、进程列表、Node.js 进程高亮。颜色以系统默认监控界面风格为主，理性、清晰、适合论文插图。不要概念艺术，不要科幻 HUD，不要卡通风。
```

### 负向提示词

```text
不要科幻面板，不要发光边框，不要大面积霓虹，不要卡通图标，不要插画风，不要复杂 3D 透视，不要把界面做成手机 App，不要夸张动画效果，不要无意义英文乱码
```

### 备用精简版提示词

```text
真实 Windows 任务管理器和资源监测面板风格的论文示意图，左右对比：左侧 100 客户端广播场景，显示 CPU 2.1%、工作集 161MB、私有内存 187MB、持续时间 1.5s；右侧 AI 问答场景，显示 CPU 低于1%、工作集 93MB、私有内存 118MB、持续时间 31.0s。横向 16:9，清晰可读，非插画，适合毕业论文。
```

## 四、如果你要更像“测试报告插图”

你还可以在提示词末尾追加这些限定语：

```text
带有轻微屏幕截图质感，字体规整，像真实 Windows 软件界面截图，适合直接插入毕业论文第六章性能测试部分
```

## 五、如果你要更像“统计结果页”

可以在提示词末尾追加：

```text
强调控制台输出和统计摘要，弱化装饰元素，保证数字和标签最醒目
```

## 六、如果你后面真的要放进论文

建议把第六章对应图名和描述同步改掉，例如：

- 图6-4 并发广播测试示意图
- 图6-5 资源占用监测示意图

正文里也把“证据图”“日志图”“真实截图”改成“示意图”或“可视化示意图”，避免图文不一致。
